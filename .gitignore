# Build
/build*/
out/
cmake-build-*/
*.bgar
.devlog

/node_modules/
.shots
/dist/
*.hdr

# IDE
.vs/
.vscode/
.idea/
*.user
*.suo
*.sln
*.vcxproj*
compile_commands.json

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Skia pre-built (large binaries) and source checkout
third_party/skia/lib/
third_party/skia/include/
third_party/skia/src/

# Generated Vulkan import lib (MinGW only)
third_party/libvulkan-1.a
third_party/vulkan-1.def

# Steamworks SDK — proprietary, non-redistributable. Supplied out of band for
# BRO_WITH_STEAM=ON builds; the redistributable ships with the product only.
# The MIT repo stays clean (handoff steam_lab §3.1 / §8).
third_party/steamworks/
# Dev Steam app id (480 = Spacewar) dropped next to the executable — never ship.
steam_appid.txt
**/steam_appid.txt

# Temp files
*.log
build_log.txt
build.bat


# app runtime
.storage.json

# Test scratch dir (rigging glTF round-trip etc.)
tests/tmp/
