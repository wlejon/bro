# Shadow DOM headless tests
wait 100

# Test 1: Basic shadow DOM - fancy-card should exist and have shadow content
eval document.querySelector('fancy-card') !== null
eval document.querySelector('fancy-card').shadowRoot !== null
eval document.querySelector('fancy-card').shadowRoot.mode

# Test 2: Shadow root innerHTML
eval document.querySelector('fancy-card').shadowRoot.innerHTML.indexOf('card-title') >= 0

# Test 3: Shadow root querySelector
eval document.querySelector('fancy-card').shadowRoot.querySelector('.card-title') !== null
eval document.querySelector('fancy-card').shadowRoot.querySelector('.card-title').textContent

# Test 4: Style encapsulation - outer .title should NOT be affected by shadow .title
eval document.querySelector('#outer-title') !== null
eval document.querySelector('#outer-title').className

# Test 5: encap-demo has its own shadow root
eval document.querySelector('encap-demo').shadowRoot !== null
eval document.querySelector('encap-demo').shadowRoot.querySelector('.title').textContent

# Test 6: Slot projection - info-panel
eval document.querySelector('info-panel') !== null
eval document.querySelector('info-panel').shadowRoot !== null
eval document.querySelector('info-panel').shadowRoot.querySelector('.header') !== null

# Test 7: Event retargeting - event-demo
eval document.querySelector('event-demo').shadowRoot !== null
eval document.querySelector('event-demo').shadowRoot.querySelector('#shadow-btn-a') !== null

# Test 8: Dynamic shadow DOM
eval var d = document.createElement('div'); d.attachShadow({mode:'open'}); d.shadowRoot !== null
eval var d = document.createElement('div'); var sr = d.attachShadow({mode:'open'}); sr.innerHTML = '<div id="t">hello</div>'; sr.querySelector('#t').textContent

# Test 9: Nested components
eval document.querySelector('outer-comp') !== null
eval document.querySelector('outer-comp').shadowRoot !== null

# Test 10: ShadowRoot host getter
eval document.querySelector('fancy-card').shadowRoot.host === document.querySelector('fancy-card')

# Test 11: Closed mode
eval var d = document.createElement('div'); d.attachShadow({mode:'closed'}); d.shadowRoot === null

# Test 12: Cannot attach twice
eval var d = document.createElement('div'); d.attachShadow({mode:'open'}); try { d.attachShadow({mode:'open'}); 'no error'; } catch(e) { 'error: ' + e.message; }

quit
